[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    slow: 실제 영상 전체 스캔이 필요한 통합 테스트 (수 분 소요)
    ocr: PaddleOCR 설치 및 모델 다운로드 필요

# 기본 실행 시 slow / ocr 마커 제외 (빠른 테스트만)
addopts = -m "not slow and not ocr" -v
